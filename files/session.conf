# written by install.sh — read by lib/common.sh before it fills in
# any default, because hooks, cron and tmux inherit no shell environment.
SESSION_DATA_DIR="${SESSION_DATA_DIR:-$HOME/roost/claude/usage}"
SESSION_TMUX_MAIN_GUARD="${SESSION_TMUX_MAIN_GUARD:-$HOME/roost/claude/lib/tmux-main-guard.sh}"
SESSION_ATTEND_GRACE="${SESSION_ATTEND_GRACE:-300}"
SESSION_ATTEND_TAIL="${SESSION_ATTEND_TAIL:-10}"
SESSION_PRIMARY_CFG="${SESSION_PRIMARY_CFG:-$HOME/roost/claude}"
