#!/bin/bash
# Configure and re-enable unattended-upgrades (stopped at start of setup
# to prevent dpkg lock conflicts). Already pre-installed on Ubuntu 24.04.
source "$(dirname "$0")/../_setup-env.sh"

cat > /etc/apt/apt.conf.d/50unattended-upgrades << 'EOF'
Unattended-Upgrade::Allowed-Origins {
    "${distro_id}:${distro_codename}-security";
    "${distro_id}ESMApps:${distro_codename}-apps-security";
    "${distro_id}ESM:${distro_codename}-infra-security";
};
Unattended-Upgrade::AutoFixInterruptedDpkg "true";
Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
Unattended-Upgrade::Remove-Unused-Dependencies "true";
EOF

cat > /etc/apt/apt.conf.d/20auto-upgrades << 'EOF'
APT::Periodic::Update-Package-Lists "1";
APT::Periodic::Unattended-Upgrade "1";
APT::Periodic::AutocleanInterval "7";
EOF

systemctl unmask unattended-upgrades
systemctl start unattended-upgrades
ok "Unattended security upgrades configured and re-enabled"
